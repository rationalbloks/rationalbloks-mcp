# RationalBloks MCP Server - Smithery Configuration
# Deploy production APIs in minutes
# One-click installation: npx @smithery/cli install @rationalbloks/mcp

name: rationalbloks
displayName: RationalBloks
version: 0.6.0
description: Deploy production APIs in minutes. 18 tools for projects, schemas, and deployments.
author: RationalBloks
homepage: https://rationalbloks.com
repository: https://github.com/rationalbloks/rationalbloks-mcp
icon: https://rationalbloks.com/logo.svg
documentation: https://rationalbloks.com/docs/mcp

# Configuration schema for Smithery UI
configSchema:
  type: object
  title: RationalBloks Configuration
  required: []
  properties:
    apiKey:
      type: string
      title: API Key
      description: Your RationalBloks API key (get it from rationalbloks.com/settings)
      default: ""
    timeout:
      type: number
      title: Timeout
      description: Request timeout in seconds
      default: 30
    logLevel:
      type: string
      title: Log Level
      description: Logging level
      default: INFO
      enum:
        - DEBUG
        - INFO
        - WARNING
        - ERROR

startCommand:
  type: stdio
  command: uvx
  args:
    - rationalbloks-mcp
  env:
    RATIONALBLOKS_API_KEY:
      description: "Your RationalBloks API Key (get it from rationalbloks.com/settings)"
      required: false
      default: ""
    RATIONALBLOKS_TIMEOUT:
      description: "Request timeout in seconds"
      required: false
      default: "30"
    RATIONALBLOKS_LOG_LEVEL:
      description: "Logging level (DEBUG, INFO, WARNING, ERROR)"
      required: false
      default: "INFO"
