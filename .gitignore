# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Testing
.pytest_cache/
.coverage
htmlcov/

# Build
*.whl

# UV — uv.lock IS committed: this package is deployed via Docker `uv sync`, so a
# pinned lock gives reproducible builds and is the second guard (after the pyproject
# bounds) against ever resolving a breaking SDK major like mcp 2.0.

# MCP Publisher
mcp-publisher.exe
mcp-publisher
.mcpregistry_*
